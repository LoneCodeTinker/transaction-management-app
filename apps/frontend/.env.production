# Production Environment Configuration
# In production, API calls should route through the /api path or absolute URL
# This can be overridden at build time: VITE_API_BASE_URL=https://api.example.com vite build
VITE_API_BASE_URL=/api
